        window.addEventListener("DOMContentLoaded",function(){
            
            // 로드구역확인!
            console.log("로딩완료!");
            
            // 구현내용: 이동버튼 클릭시 이미지 이동하여 변경하기
            // 이벤트 종류: click
            // 이벤트 대상: .abtn
            // 변경 대상: .gbox, .gbox img
            ////////////////////////////////////////////
            
            // 이벤트 대상
            var abtn = document.querySelectorAll('.cir_off2 li');
            
            // 변경 대상
            var gbox = document.querySelector('.ban_box');
            
            // 왼쪽버튼 클릭시 함수호출(전달값0)
             // 왼쪽버튼 클릭시 함수호출(전달값0)
            abtn[0].onclick = function(){
                
                // 함수호출확인!
                //console.log("나 왼쪽버튼이얌!");
                
                // 자동호출지우기
                clearAuto();
                
                
                
                // a태그 기본이동 막기
                return false;
                
            };////// 클릭함수 /////////////////
            // 오른버튼 클릭시 함수호출(전달값1)
      
     
            
            
            /*/////////////////////////////////////////
                함수명: chgImg
                기능: 이미지를 이동하여 이미지 순서를 변경한다!
            *//////////////////////////////////////////
            // 익명함수가 변수에 할당된 함수임!! /////////
            
            // 광클금지 상태변수 ////////////////
            var prot = 0;//0-허용, 1-막기(문닫기)
            // 한번 선언되면 끝
            // 호출될때마다 실행되는건 함수 안에서만!!
            
            var chgImg = function(dir){// dir = 방향(0-왼쪽, 1-오른쪽)
                
                //console.log("광클상태값1 :"+prot);
                
                // 0.광클금지 ////////////////////////////
                
                // (1) if문으로 상태값 막기!
                if(prot) return false;//돌아가!
                // prot===1이면 돌아가
                
                // (2) 상태값 막기(1)로 변경
                prot = 1;//상태값 1로 변경!
                //console.log("광클상태값2 :"+prot);
                
                // (3) 작동시간 후에 광클상태값을 0으로 풀어주기
                setTimeout(function(){
                    prot=0;//상태풀기
                    //console.log("광클상태값3 :"+ prot);
                                     
                 },400);
                // setTimeout(함수,시간) - 설정시간 후 한번 함수호출!
                // 시간이 400이면 0.4초이므로 이미지가 넘어가는 
                // 트랜지션 시간인 0.4초간 광클을 막아준다!
                ////////////////// 광클 금지 ////////////
                
                // 1. 함수호출 및 전달값 확인
                //console.log("나야나!"+dir);
                
                // 2. 변경된 img리스트 읽어오기
                var ilist = gbox.querySelectorAll('img');
                // 첫번째 이미지
                var fele = ilist.item(0);
                // 마지막 이미지
                var lele = ilist.item(ilist.length-1);
                
                
                // 3.오른쪽버튼 클릭시 맨앞요소 맨뒤로 이동하기
                if(dir){ // 1이면 true
                    // 맨앞요소 맨뒤로 이동
                    gbox.appendChild(fele);
                    
                }////// if //////////////////////////
                
                // 4.왼쪽버튼 클릭시 맨뒤요소 맨앞으로 이동하기
                else{
                    gbox.insertBefore(lele,fele);
                    
                }///// else ////////////////////////
                
            }/////// chgImg 함수 /////////////////////
            /////////////////////////////////////////
            /*
            버튼을 동반한 배너형 컨텐츠에서 다루는 4가지 중요분야
            1. 광클금지
               - 버튼 클릭을 빨리하여 기존 기능이 망가져 보이는 것을 막음
            2. 자동넘기
               - 일정시간 간격으로 배너가 넘어감
            3. 버튼 클릭시 자동 넘김 지우기
              - 사용자가 버튼을 조장하는 순간 자동넘김 기능 지우기
            4. 사용자 조작이 끝난 후 다시 자동 넘기기
              - 버튼 마지막 클릭이 끝난 후 자동 넘김 셋팅 하나만 남기기
            */
            
        /*/////////////////////////////////////////
            함수명: autoCall
            기능: 슬라이드 넘기기 함수 자동호출(인터발함수)
        *//////////////////////////////////////////
        var autoI;//인터발용변수
        ///////////////////////////////////////////
        var autoCall = function(){
            
            console.log("자동호출작동!");
            
            autoI = setInterval(function(){
                chgImg(1)
            },3000);
            //setInterval(함수,시간)
            //일정시간 간격으로 함수를 호출하는 JS내장함수
        };///// autoCall 함수 /////////////////////
        //////////////////////////////////////////    
            
        /// 자동넘김 최초호출(반드시 할당형 익명함수 아래에서 호출!)
        autoCall();
        
        /*/////////////////////////////////////////////////
            함수명: clearAuto
            기능: 인터발함수를 지우고, 잠시 후 다시 자동호춠셋팅하기
                 이동버튼 클릭시 이 함수를 호출하여야 함
        *//////////////////////////////////////////////////
        var autoT;//타임아웃용변수
        ///////////////////////////////////////////////
        var clearAuto = function(){
            
            console.log("자동호출지우기!");
            
            // 인터발함수 지우기
            clearInterval(autoI);
            
            // 타임아웃함수 지우기(실행쓰나미방지!)
            clearTimeout(autoT);
            
            // 일정시간 후 자동호출함수 재호출하기
            autoT = setTimeout(autoCall,3000)
            
        };///////// clearAuto함수 ////////////////////
        /////////////////////////////////////////////
            
        });//// 로드 구역/////////////////////////////
        ///////////////////////////////////////////







///// 로드구역 //////////////////////////////
    $(function(){
                  
        var sld2 = $(".ban_box");
        var sldCnt2 = sld2.find("img").length;
        
        
        var sldSts2 = 0;//슬라이드 상태값
        // 0 자동상태, 1 멈춤상태
        
        $(".cir_off2 li").first().click(function(){
            console.log("멈춤버튼");
            var v1,v2;
            if(sldSts2===0){
                v1=1;
                v2=0;
                
                clearInterval(autoI);
                
                
            } 
            else{
                v1=0;
                v2=1;
                
                autoSlide();
                
            }
            
            $("img:last-child",this).css({
                opacity: v1
            });
            
            $("img:first-child",this).css({
                opacity: v2
            });
            
            // 상태값 변경
            sldSts2 ? sldSts2 = 0 : sldSts2 = 1;            
        });////// click //////////////////////
                // 슬라이드전역변수    
    var snum = 0;    
         // 인터발함수(지우기위해 만든변수)
    var autoI;
    /*////////////////////////////////////////////
        함수명: autoSlide
        기능: 자동넘기기 셋팅함수(인터발함수)
    */ ////////////////////////////////////////////
    var autoSlide = function () {
        autoI = setInterval(function () {
            snum++;
            if(snum===sldCnt2)snum=0;
            
            sldFn2(snum);
            
        }, 3000);
        
    }; ////// autoSlide함수 //////////////////////
    //////////////////////////////////////////////
        
        
        // 할당형함수 아래에서 처음 호출해야함!
    autoSlide(); //최초호출!


    // 타임아웃변수(쓰나미방지용 지우기변수)
    var autoT;
    /*////////////////////////////////////////////
        함수명: clearAuto
        기능: 자동넘김 지우기함수(클리어인터발함수)
    */ ////////////////////////////////////////////
    var clearAuto = function () {

        // 자동넘김 할당된 변수를 지운다(autoI)
        clearInterval(autoI);
        // 타임아웃 실행쓰나미 방지용 타임아웃 지우기를 꼭한다!
        clearTimeout(autoT);

        // 안건드리면 5초후 다시 자동호출하기!
        // 매번 호출될때 위에서 지우므로 단 하나만 남아있음!
        autoT = setTimeout(autoSlide, 5000);

    }; ////// clearAuto함수 /////////////////////////


        
    ////////////////////////////////////////////////
    });/////////////// 로드구역 ///////////////////////
///////////////////////////////////////////////////////
///////////////////////////////////////////////////////